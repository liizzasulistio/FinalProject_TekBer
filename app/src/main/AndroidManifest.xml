<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myclassroom" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyClassroom" >
<!--        <activity-->
<!--            android:name=".screens.MenuActivity"-->
<!--            android:label="@string/title_activity_menu"-->
<!--            android:theme="@style/Theme.MyClassroom.NoActionBar" >-->
<!--        </activity>-->
        <activity android:name=".screens.TambahKelasActivity" />
        <activity android:name=".screens.ListKelasActivity" />
        <activity android:name=".screens.RegisterActivity" />
        <activity android:name=".screens.TestActivity" />
        <activity
            android:name=".screens.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".screens.SplashScreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>